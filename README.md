# principios_prog_web_25.2
# LED Marketplace

## 📝 Descrição

Este projeto é um e-commerce no estilo marketplace focado na compra e venda de produtos de iluminação LED. A plataforma permitirá que usuários se cadastrem tanto como compradores quanto como vendedores, criando um ambiente dinâmico para negociação de produtos, semelhante a grandes marketplaces como o Mercado Livre.

## ✨ Funcionalidades Principais (Planejadas)

-   **Cadastro de Usuários:** Sistema de autenticação para compradores e vendedores.
-   **Listagem de Produtos:** Vendedores podem cadastrar seus produtos de LED, com descrições, preços e imagens.
-   **Busca e Filtro:** Usuários podem buscar por produtos específicos, filtrar por categoria, preço, etc.
-   **Carrinho de Compras:** Funcionalidade para adicionar múltiplos produtos antes de finalizar a compra.
-   **Gerenciamento de Pedidos:** Painel para compradores e vendedores acompanharem o status dos seus pedidos.
-   **Sistema de Avaliação:** Usuários podem avaliar produtos e vendedores.

## 🛠️ Tecnologias Utilizadas

-   **Backend:**
    -   [Node.js](https://nodejs.org/)
    -   [Express.js](https://expressjs.com/)
-   **Frontend:**
    -   HTML5
    -   CSS3
    -   JavaScript

## 🚀 Como Executar o Projeto

Siga os passos abaixo para configurar e executar o projeto em seu ambiente local.

1.  **Clone o repositório:**
    ```bash
    git clone https://github.com/Simon60679/principios_prog_web_25.2.git
    ```

2.  **Navegue até a pasta do projeto:**
    ```bash
    cd principios_prog_web_25.2
    ```

3.  **Instale as dependências do backend:**
    *(Assumindo que o `package.json` está na raiz ou em uma pasta `backend`)*
    ```bash
    # Se estiver em uma pasta 'backend'
    cd backend 
    npm install
    ```

4.  **Inicie o servidor backend:**
    ```bash
    npm start
    ```
    O servidor estará rodando em `http://localhost:3000`.

5.  **Abra o frontend:**
    -   Navegue até a pasta `frontend` (se houver uma).
    -   Abra o arquivo `index.html` no seu navegador de preferência.

