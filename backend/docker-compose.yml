services:
  postgres-db:
    image: postgres:13
    container_name: meu-postgres-db
    environment:
      POSTGRES_USER: backend
      POSTGRES_PASSWORD: web1232
      POSTGRES_DB: lab_init_web_II
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
